export const MOCK_USERNAME = 'demo'
export const MOCK_PASSWORD = 'demo'
export const MOCK_TOKEN = 'mock-token-cs-001'

/** T-009: login / me / logout use the real API. Other mocks still key off MOCK_TOKEN. */
export function installAuthMocks(): void {
  return
}
