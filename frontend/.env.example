# 前端请求前缀。开发环境必须用相对路径 /api，由 Vite 代理转发，禁止写成完整后端 URL
VITE_API_BASE_URL=/api
# 仅供 Vite 开发代理使用，前端业务代码不得读取或写死后端端口
VITE_BACKEND_PROXY_TARGET=http://localhost:8099
# WebSocket 相对路径，由 Vite 代理 /ws，前端不得写死后端端口
VITE_WS_PATH=/ws
